{
  "name": "id-checker-bot",
  "version": "1.0.0",
  "description": "Telegram bot to check user ID, chat ID and messages",
  "main": "api/webhook.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy"
  },
  "dependencies": {
    "node-telegram-bot-api": "^0.61.0"
  },
  "devDependencies": {
    "vercel": "^32.3.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
